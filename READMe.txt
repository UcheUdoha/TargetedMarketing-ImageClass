# Fashion MNIST CNN Classification

## Introduction
This project implements a Convolutional Neural Network (CNN) to classify the Fashion MNIST dataset using Keras in both Python and R.

## Instructions

### Python

1.⁠ ⁠*Setup the Environment:*
   ```bash
   pip install keras tensorflow numpy matplotlib
2. *Import libraries:*
3. *Load the Fashion MNIST dataset:*
4. *Normalize the pixel values to be between 0 and 1:*
5. *Build the CNN model:*
6. *Compile the model:*
7. *Make predictions on test images:*
8. *Add channel dimension:*
9. *Function to plot the image and prediction:*
10. *Plot the first 2 test images with predictions:*
### Zipping the Project